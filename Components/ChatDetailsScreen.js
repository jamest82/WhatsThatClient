import React, { Component } from 'react';
import { Button, Text, View } from 'react-native';

export default class ChatDetailsScreen extends Component {

    render(){
        return(
            <View>
                <Text>Hello detailChats</Text>
            </View>
        );
    }
}